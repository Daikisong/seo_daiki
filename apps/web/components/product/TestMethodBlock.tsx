export function TestMethodBlock() {
  return (
    <section className="rounded-md border border-neutral-200 bg-white p-4">
      <h2 className="text-lg font-semibold">Test method</h2>
      <p className="mt-2 text-sm text-neutral-700">
        We capture the seller claim first, select the exact SKU, record PD/PPS profiles, run a sustained load check, and
        only reuse numbers that have evidence IDs.
      </p>
    </section>
  );
}
