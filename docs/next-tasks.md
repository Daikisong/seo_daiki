# Next Tasks

1. Connect a real Postgres database and run Prisma migration/seed.
2. Add production credentials for AliExpress, Search Console, and R2/S3 lab-evidence storage, then run the worker and DB import commands.
3. Replace generated planned pages with real evidence-rich pages as product data grows.
4. Run a Postgres-backed smoke test for admin archive/delete, audit logs, lab evidence asset selection, and worker imports.
