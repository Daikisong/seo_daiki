You are working in:

Songdaiki/seo_daiki

Goal:
Make the API-free pipeline fully work from step 1 through step 6.

The desired API-free flow is:

1. Detect country/market trends.
2. Generate trend keywords.
3. Observe top results via manual CSV / local SERP provider fixture.
4. Analyze top blog/site structures.
5. Let the LLM strategy layer decide what article should be written.
   - This must work without external LLM API keys using dry-run/local deterministic mode.
   - If an LLM provider is configured, it may be used, but it must not be required.
6. Generate website test posts and verify the generated routes render/build.

Do NOT work on:
- Search Console performance loop
- product candidate analysis
- monetization review
- AliExpress / Temu / Amazon / iHerb live API integration
- posting bots
- backlink/outreach
- distribution
- affiliate link insertion

Only make the first 6 steps robust.

Important:
Do not stop after one patch.
Run the API-free pipeline, inspect failures, patch, run again.
Repeat until the API-free step 1→6 flow passes or until a real blocker is documented.
Do not ask questions.
Do not require external credentials.
Do not require Brave API, Google API, Search Console, OpenAI, Gemini, Anthropic, Ollama, or affiliate API credentials.
Everything must work from local seed files and dry-run providers.

────────────────────────────────────
TARGET FLOW
────────────────────────────────────

The target API-free flow must be:

trend:init-markets
  ↓
trend:import-signals
  ↓
trend:normalize
  ↓
trend:cluster
  ↓
trend:score
  ↓
trend:generate-keywords
  ↓
trend:report
  ↓
serp:import-results
  ↓
serp:fetch-pages
  ↓
serp:analyze-pages
  ↓
serp:summarize-opportunity
  ↓
serp:report
  ↓
strategy:create
  ↓
strategy:generate-brief
  ↓
post:generate-test
  ↓
post:publish-test --mode noindex
  ↓
verify generated website routes
  ↓
build passes

This is API-free and must be executable with:

pnpm pipeline:api-free-six

or, if you keep the existing name:

pnpm pipeline:trend-to-post

But add a dedicated verification command:

pnpm verify:api-free-six

The verification command must fail loudly if any of the 6 phases did not produce expected outputs.

────────────────────────────────────
PHASE A — AUDIT CURRENT API-FREE FLOW
────────────────────────────────────

First inspect the current repo and write:

docs/api-free-six-audit.md

It must answer:

1. Which current commands correspond to steps 1–6?
2. Which commands currently require APIs?
3. Which commands only use seed CSV / local JSON?
4. Which outputs are generated by each step?
5. Which generated outputs are consumed by the next step?
6. Which step currently breaks, if any?
7. Which web routes should render the generated test posts?
8. Which scripts need to be added or corrected?

Inspect at minimum:
- README.md
- package.json
- workers/python/cli.py
- workers/python/pipeline.py
- workers/python/pipeline_trend_steps.py
- workers/python/intelligence/market_trend_engine.py
- workers/python/intelligence/market_trend_*.py
- workers/python/serp/serp_intelligence.py
- workers/python/serp/*.py
- workers/python/writers/market_content_strategy.py
- workers/python/writers/market_content_*.py
- workers/python/writers/market_test_*.py
- apps/web/app/[market]/[language]/posts/[slug]/page.tsx
- apps/web/lib/market/market-data*.ts
- apps/web/lib/seo/metadata.ts
- data/seeds/*.csv

────────────────────────────────────
PHASE B — DEFINE EXACT SUCCESS CRITERIA
────────────────────────────────────

Add docs/api-free-six-success-criteria.md.

The API-free 1→6 flow passes only if all of these are true:

Trend outputs:
- data/exports/market_trend_sources.json exists
- data/exports/market_trend_signals.json exists
- data/exports/trend_clusters.json exists
- data/exports/trend_keywords.json exists
- data/exports/trend_report.json exists
- at least 5 market trend clusters exist
- at least 5 trend keywords exist
- at least 3 distinct markets exist in the generated outputs

SERP outputs:
- data/exports/serp_results.json exists
- data/exports/competitor_content_analysis.json exists
- data/exports/serp_opportunity_report.json exists
- at least 5 SERP results exist
- at least 5 competitor analyses exist
- at least 3 SERP opportunities exist
- each opportunity has:
  - keywordId
  - market
  - language
  - keyword
  - dominantIntent
  - recommendedAngle
  - recommendedArticleType
  - shouldWrite

Strategy outputs:
- data/exports/content_strategies.json exists
- data/exports/content_briefs.json exists
- at least 3 content strategies exist
- at least 3 content briefs exist
- each strategy has:
  - keywordId
  - clusterId
  - market
  - language
  - selectedArticleType
  - recommendedAngle
  - titleStrategy
  - sectionPlanJson
  - monetizationDeferred = true

Test post outputs:
- data/exports/test_articles.json exists
- at least 3 test articles exist
- each test article has:
  - market
  - language
  - slug
  - title
  - h1
  - metaDescription
  - contentMdx or sections
  - affiliateLinks = []
  - monetizationDeferred = true
  - productCandidateState = pending
  - indexStatus = noindex
  - publishStatus = pending or published/noindex depending on command
- test posts must not contain affiliate links.

Website route verification:
- For each generated test article, the route path should be:
  /[market]/[language]/posts/[slug]/
- The web layer should be able to read generated test_articles.json.
- generateStaticParams for posts should include generated posts.
- post metadata must be noindex.
- marketResearchMetadata must be used or equivalent noindex metadata must exist.
- pnpm build must pass.

Safety:
- No live affiliate APIs are called.
- No Search Console API is required.
- No external LLM API is required.
- No raw Google scraping is required.
- No product monetization is inserted.
- No distribution/outreach is run.

────────────────────────────────────
PHASE C — ADD A DEDICATED API-FREE PIPELINE COMMAND
────────────────────────────────────

Add or confirm this script in package.json:

"pipeline:api-free-six": "python3 workers/python/cli.py pipeline:api-free-six"

If you prefer to alias existing flow:

"pipeline:api-free-six": "python3 workers/python/cli.py pipeline:trend-to-post --publish-test-noindex"

Implement CLI command:

pipeline:api-free-six

It must run exactly:

1. trend:init-markets
2. trend:import-signals --file data/seeds/trend-signals.csv
3. trend:normalize
4. trend:cluster
5. trend:score
6. trend:generate-keywords
7. trend:report
8. serp:import-results --file data/seeds/serp-results.csv
9. serp:fetch-pages
10. serp:analyze-pages
11. serp:summarize-opportunity
12. serp:report
13. strategy:create
14. strategy:generate-brief
15. post:generate-test
16. post:publish-test --mode noindex

It must NOT run:
- product candidate analysis
- monetization review
- offer matching
- distribution
- outreach
- live affiliate APIs
- Search Console import

Write report:
data/exports/pipeline_api_free_six_run.json

Report must include:
- step name
- status
- started_at
- finished_at
- output path
- error if failed

If any step fails, the pipeline fails and prints the report path.

────────────────────────────────────
PHASE D — ADD VERIFY COMMAND
────────────────────────────────────

Add package script:

"verify:api-free-six": "python3 workers/python/cli.py verify:api-free-six"

Implement CLI command:

verify:api-free-six

It should load generated files and assert all success criteria from docs/api-free-six-success-criteria.md.

Write:
data/exports/api_free_six_verification.json

Include:
{
  "passed": true/false,
  "checks": [
    {
      "name": "...",
      "status": "pass" | "fail",
      "detail": "..."
    }
  ],
  "generatedRoutes": [
    "/us/en/posts/...",
    "/es/es/posts/...",
    "/br/pt-br/posts/..."
  ]
}

If a check fails:
- exit non-zero
- print exactly what failed
- do not silently pass.

────────────────────────────────────
PHASE E — SEED DATA MUST BE COMPLETE
────────────────────────────────────

Ensure these files exist and contain enough data:

data/seeds/trend-signals.csv
data/seeds/serp-results.csv
data/seeds/competitor-page-summaries.csv

The seed data must cover at least 5 markets:

1. US / en
- trend: magnesium sleep

2. ES / es
- trend: cargador usb c 65w

3. BR / pt-br
- trend: power bank capacidade real

4. JP / ja
- trend: compact desk gadget

5. KR / ko
- trend: gut health

Each market should have:
- at least 1 trend signal
- at least 1 keyword generated
- at least 1 SERP snapshot
- at least 1 competitor page summary
- at least 1 content strategy
- at least 1 test post

SERP seed rows must include:
- rank
- url
- domain
- title
- snippet
- result_type
- is_forum
- is_video
- is_ecommerce
- is_affiliate_likely
- is_publisher
- language_guess

Competitor summary seed rows must include:
- url
- h1
- headings
- word_count_estimate
- content_type_guess
- intent_served
- monetization_pattern
- affiliate_pattern
- comparison_table_present
- product_links_present
- original_data_present
- freshness_signals
- content_angles
- missing_angles
- strengths
- weaknesses

Do not require external fetching.

────────────────────────────────────
PHASE F — STRATEGY ENGINE MUST WORK WITHOUT LLM API
────────────────────────────────────

The step “LLM decides what article to write” must work without external keys.

Implement this mode:

LLM_PROVIDER=dry-run

or, if the repo uses another name, create a deterministic strategy refinement layer called:

dry_run_strategy_refiner

Input:
- market config
- trend cluster
- trend keyword
- SERP opportunity
- competitor analyses
- top patterns
- content gaps
- forbidden claims
- monetizationDeferred=true

Output:
- selectedArticleType
- recommendedAngle
- titleStrategy
- h1
- metaDescription
- introStrategy
- sectionPlanJson
- differentiationPlanJson
- evidenceNeededJson
- productCandidateNeedsJson
- forbiddenClaimsJson

Rules:
- No affiliate links.
- No prices.
- No product claims.
- No medical claims.
- No “we tested” unless evidence exists.
- No copied competitor wording.
- Always include “Product candidate analysis pending” or equivalent state.

If real LLM provider exists, keep it optional.
Dry-run must be default for API-free pipeline.

────────────────────────────────────
PHASE G — TEST POST WEB ROUTE MUST BE VALID
────────────────────────────────────

Patch web route if needed:

/[market]/[language]/posts/[slug]/

Requirements:
- Reads data/exports/test_articles.json through current market-data reader.
- Renders title, summary, sections.
- Shows productCandidateState.
- Shows monetizationDeferred state.
- Uses marketResearchMetadata or equivalent robots noindex.
- Canonical points to /[market]/[language]/posts/[slug]/.
- Hreflang only includes existing variants.
- No affiliate links.
- If the test article does not exist, return notFound.

Add route/module tests if missing:
- test generated post route model
- test post metadata noindex
- test generated slug path
- test no affiliate links in test post

────────────────────────────────────
PHASE H — PATCH UNTIL IT WORKS
────────────────────────────────────

After implementing, run:

pnpm pipeline:api-free-six
pnpm verify:api-free-six
pnpm test:python
pnpm test:seo-route-modules
pnpm test:seo
pnpm seo:validate
pnpm typecheck
pnpm build

If any command fails:
1. Read the error.
2. Patch the cause.
3. Re-run the failing command.
4. Continue until the API-free six-step flow passes.

Do not stop after documenting the failure unless it is a real external environment blocker.
If the blocker is missing dependency or script mismatch, patch it.
If the blocker is external API, bypass with dry-run/manual provider because this phase must not need APIs.

────────────────────────────────────
PHASE I — FINAL REPORT
────────────────────────────────────

Write:

docs/api-free-six-final-report.md

Include:
1. Summary of whether steps 1–6 now pass.
2. Commands run.
3. Outputs generated.
4. Generated test post routes.
5. Which files were patched.
6. Which steps are still dry-run/manual.
7. Which APIs are intentionally not required.
8. Remaining weaknesses.
9. Exact next step after API-free step 1–6 passes.

Also write:

data/exports/api_free_six_capabilities.json

With:
{
  "country_trend_detection": true,
  "trend_keyword_generation": true,
  "serp_observation_api_free": true,
  "competitor_content_structure_analysis": true,
  "dry_run_llm_content_strategy": true,
  "website_test_posting": true,
  "search_console_required": false,
  "affiliate_api_required": false,
  "brave_api_required": false,
  "external_llm_required": false,
  "monetized_links_inserted": false,
  "human_approval_required_for_monetization": true
}

Final answer must be concise:
- pass/fail
- commands run
- generated routes
- remaining issues